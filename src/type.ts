export interface Product {
    quantity: string | number | readonly string[] | undefined;

    id: string;
    title: string;
    description: string;
    category: string;
    price: number;
  
    
  
}